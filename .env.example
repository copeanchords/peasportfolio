# Copy to .env.local and fill in real values (never commit .env.local).
# See scripts/get-spotify-refresh-token.mjs for how to obtain these.
SPOTIFY_CLIENT_ID=
SPOTIFY_CLIENT_SECRET=
SPOTIFY_REFRESH_TOKEN=
